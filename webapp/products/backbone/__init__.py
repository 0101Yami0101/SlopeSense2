"""Data Backbone — the pipeline every module stands on.

Not a forecast, which is why it is a "foundation" card rather than a product
one. It is here because the pipeline is the actual asset: the modules above it
are what the pipeline can be pointed at, and the second one costs a fraction
of the first precisely because this layer is shared.
"""
